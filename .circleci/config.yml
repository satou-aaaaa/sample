version: 2.1

jobs:
  build:
    orbs:
      gradle: circleci/gradle@3.0.0
    steps:
      - checkout
      - run: ./gradlew build
  test:
    steps:
      - checkout
      - run: ./gradlew test
workflows:
  jobs:
    - build
    - test