version: 2.1

jobs:
  test:
    docker:
      - image: cimg/openjdk:20.0.1
        auth:
            username: $DOCKER_HUB_USER
            password: $DOCKER_HUB_PASSWORD
    steps:
      - checkout
      - run: |
          gradle test

workflows:
  test:
    jobs:
      - test