version: 2.1

jobs:
  build:
    steps:
      - checkout
      - run: ./gradlew build
  test:
    steps:
      - checkout
      - run: ./gradlew test
workflows:
  build_and_test:
    jobs:
      - build
      - test

